/**
 * 
 */
(function($) {

  $(document).ready(function () {
    $('button.proxy-button').once().click(function () {
      var targetName = $(this).data('targetName');
      
      if (targetName) {
        $('[name="' + targetName + '"]').click();
      }
    });

  });
  
})(jQuery);